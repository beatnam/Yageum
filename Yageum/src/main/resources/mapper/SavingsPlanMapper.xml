<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yageum.mapper.SavingsPlanMapper">

    <select id="getLatestSavingsPlanByMemberIn" resultType="map">
        SELECT
            plan_in,
            member_in,
            plan_name,
            plan_amount,
            plan_feedback,
            plan_created_date,
            plan_target_date
        FROM savings_plan
        WHERE member_in = #{memberIn}
        ORDER BY plan_created_date DESC
        LIMIT 1
    </select>

    <select id="getAllSavingsPlansByMemberIn" resultType="map">
        SELECT
            plan_in,
            member_in,
            plan_name,
            plan_amount,
            plan_feedback,
            plan_created_date,
            plan_target_date
        FROM savings_plan
        WHERE member_in = #{memberIn}
        ORDER BY plan_created_date DESC
    </select>

</mapper>